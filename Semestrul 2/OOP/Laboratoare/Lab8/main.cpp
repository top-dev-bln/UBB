#include "ui.h"
#include "tests.h"
#include "service.h"
#include "repository.h"
#include "validator.h"
#include "wishlist.h"

int main() {
    testall();
   // Repository repo;
    //Wishlist wishlist;
    //Validator validator(repo);
    //Service servic(repo, validator, wishlist);
    //UI ui(servic);
    //ui.run();
    return 0;
}